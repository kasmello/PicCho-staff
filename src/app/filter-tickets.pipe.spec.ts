import { FilterTicketsPipe } from './filter-tickets.pipe';

describe('FilterTicketsPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterTicketsPipe();
    expect(pipe).toBeTruthy();
  });
});
